# MLB-Modelling uses the Python standard library only (3.9+).
# There are no third-party runtime dependencies — this file is intentionally
# empty so `pip install -r requirements.txt` is a harmless no-op in CI.
#
# Optional: `curl_cffi` improves reach on some bookmaker endpoints in src/odds.py,
# but odds is a best-effort stage and the pipeline runs fine without it.
