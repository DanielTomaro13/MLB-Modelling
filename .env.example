# MLB-Modelling — environment for the local odds cron (src/odds.py).
# Copy to `.env` (git-ignored). All optional; odds is best-effort.

# Some books impersonate a browser via curl_cffi; nothing here is required for
# Sportsbet (public apigw JSON). Add per-book tokens here if you extend odds.py.
# TAB_CLIENT_ID=
# TAB_CLIENT_SECRET=
# DABBLE_BEARER=
